upload a local folder
git add .
git commit -m "message"
git remote add origin link (first time)
git remote -v (To check link)
git push -u origin master (first time), or else git push

git checkout -b "branchname" (to create new branch and shift to that, remove -b if you want to move to existing branch)
git add .
git commit -m "message"
git push -u origin branchname (first time), or else git push



